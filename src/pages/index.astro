---
import BaseLayout from "@/layouts/BaseLayout.astro";
import BlogEntries from "@/components/entries/BlogEntry.astro";
import Cta from "@/components/global/Cta.astro";
import { getCollection } from "astro:content";
import { Image } from "astro:assets";
import banner from "../images/banner.png";
import logo from "../images/logo-circular.png";
import logoYoutube from "../images/logo-youtube.png";
const allPosts = await getCollection("posts");
const tags = [...new Set(allPosts.map((post) => post.data.tags).flat())];
// Sort posts by date in descending order (newest first)
const sortedPosts = allPosts.sort(
  (a, b) => new Date(b.data.pubDate) - new Date(a.data.pubDate)
);
---

<BaseLayout title="Discusiones Alrededor de la Biblia" displayBanner={true}>
  <section>
    <div class="py-24">
      <p>
        <strong>Bienvenido Bíblicas</strong>, un espacio donde la pasión por la Biblia se une al análisis profundo y el debate respetuoso.
        <br />
        <br />
        <strong>Aquí encontrarás:</strong>
        <br />
        <br />
        <ul>
          <li><strong>Episodios:</strong> Explora una amplia gama de temas bíblicos a través de análisis, debates y entrevistas con expertos.</li>
          <li><strong>Recursos:</strong> Accede a materiales descargables, artículos y enlaces a recursos adicionales para profundizar en tu estudio.</li>
          <li><strong>Comunidad:</strong> Únete a nuestro foro en línea y participa en discusiones con otros apasionados por la Biblia.</li>
        </ul>
        <br />
        <br />
        <strong>¿Estás listo para embarcarte en un viaje de descubrimiento bíblico?</strong> Suscríbete a nuestro podcast, explora nuestro sitio web y únete a la comunidad de "Discusiones Bíblicas".
      </p>
      <br />
      <br />
      <a href="https://www.youtube.com/@discusionesbiblicas">
        <div class="flex flex-row gap-4 border border-base-200 rounded-lg p-4">
          <div class="flex flex-col gap-2">
            <p class="text-lg font-medium">Discusiones Bíblicas</p>
            <p class="text-base text-base-600">Somos dos entusiastas por las conversaciones controvertidas. 
            Nos emociona poder descubrir diferentes interpretaciones bíblicas que nos hagan cambiar nuestra manera de pensar.</p>
            <div class="flex flex-row gap-2 items-center">
              <Image
                width={144}
                height={144}
                src={logoYoutube}
                alt="Discusiones Alrededor de la Biblia"
                class="w-8 h-8 object-cover rounded-full"
              />
              <p class="text-base text-base-600">https://www.youtube.com/@discusionesbiblicas</p>
            </div>
          </div>
          <Image
            width={512}
            height={512}
            src={logo}
            alt="Discusiones Alrededor de la Biblia"
            class="w-32 h-32 object-cover rounded-full"
          />
        </div>
      </a>
      <div class="flex flex-wrap gap-3 mt-8">
        {
          tags.map((tag) => (
            <a
              class=" hover:text-blue-500 text-xs font-medium text-base-600"
              href={`/tags/${tag}`}
            >
              {tag}
            </a>
          ))
        }
      </div>
      <div class="grid grid-cols-1 mt-4 gap-8 gap-y-24 sm:grid-cols-2">
        {
          sortedPosts.map((post) => (
            <BlogEntries
              url={"/posts/" + post.slug}
              alt={post.data.title}
              tags={post.data.tags}
              title={post.data.title}
              author={post.data.author}
              image={post.data.image.url}
              description={post.data.description}
              pubDate={post.data.pubDate.toString().slice(0, 10)}
            />
          ))
        }
      </div>
    </div>
  </section>
</BaseLayout>
